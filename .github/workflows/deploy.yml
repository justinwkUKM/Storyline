name: Deploy

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - dev
      - main

env:
  GCP_PROJECT_ID: storyline-6cd69
  GCP_REGION: asia-southeast1
  RUNTIME_SERVICE_ACCOUNT: storyline-cloud-run@storyline-6cd69.iam.gserviceaccount.com
  GEMINI_SECRET: storyline-gemini-api-key
  SESSION_SECRET: storyline-session-secret
  SHARE_SECRET: storyline-share-token-secret

jobs:
  checks:
    name: Typecheck and build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run lint

      - name: Build
        run: npm run build

  deploy-staging:
    name: Deploy staging
    runs-on: ubuntu-latest
    needs: checks
    if: github.event_name == 'push' && github.ref == 'refs/heads/dev'
    environment: staging
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          project_id: ${{ env.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v3

      - name: Deploy staging service
        run: |
          gcloud run deploy storyline-staging \
            --source . \
            --project "$GCP_PROJECT_ID" \
            --region "$GCP_REGION" \
            --allow-unauthenticated \
            --port 3000 \
            --memory 512Mi \
            --cpu 0.08 \
            --concurrency 1 \
            --min-instances 0 \
            --max-instances 2 \
            --timeout 300 \
            --execution-environment gen1 \
            --service-account "$RUNTIME_SERVICE_ACCOUNT" \
            --set-env-vars "NODE_ENV=production,FIREBASE_PROJECT_ID=$GCP_PROJECT_ID" \
            --set-secrets "GEMINI_API_KEY=$GEMINI_SECRET:latest,SESSION_SECRET=$SESSION_SECRET:latest,SHARE_TOKEN_SECRET=$SHARE_SECRET:latest"

  deploy-production:
    name: Deploy production
    runs-on: ubuntu-latest
    needs: checks
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    environment: production
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          project_id: ${{ env.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v3

      - name: Deploy production service
        run: |
          gcloud run deploy storyline \
            --source . \
            --project "$GCP_PROJECT_ID" \
            --region "$GCP_REGION" \
            --allow-unauthenticated \
            --port 3000 \
            --memory 512Mi \
            --cpu 0.08 \
            --concurrency 1 \
            --min-instances 0 \
            --max-instances 2 \
            --timeout 300 \
            --execution-environment gen1 \
            --service-account "$RUNTIME_SERVICE_ACCOUNT" \
            --set-env-vars "NODE_ENV=production,FIREBASE_PROJECT_ID=$GCP_PROJECT_ID" \
            --set-secrets "GEMINI_API_KEY=$GEMINI_SECRET:latest,SESSION_SECRET=$SESSION_SECRET:latest,SHARE_TOKEN_SECRET=$SHARE_SECRET:latest"

      - name: Deploy Firestore rules and indexes
        run: npx --yes firebase-tools@latest deploy --only firestore --project "$GCP_PROJECT_ID"
